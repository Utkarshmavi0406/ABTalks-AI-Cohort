# Copy this file to .env and adjust if needed. Never commit the real .env file.

# Inside Docker, "localhost" refers to the container itself, not your Mac --
# host.docker.internal is Docker's special hostname for reaching services
# (like Ollama) running on the host machine.
OLLAMA_BASE_URL=http://host.docker.internal:11434/v1
